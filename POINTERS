# include <stdio.h>
int main() {
    int i = 9;
    printf("Value of i : %d and its address: %u", i, &i);
    int *ptr_i;
    ptr_i = &i;
    printf("\nValue of ptr_i : %u and value pointed by the pointer is : %d", ptr_i, *ptr_i); 
    return 0;
}
####OUTPUT####
Value of i : 9 and its address: 852041940
Value of ptr_i : 852041940 and value pointed by the pointer is : 9

=== Code Execution Successful ===
